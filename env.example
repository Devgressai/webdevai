# ============================================
# EMAIL CONFIGURATION FOR CHATBOT LEADS
# ============================================

# Choose your email provider: resend, sendgrid, mailgun, webhook
EMAIL_PROVIDER=resend

# Your email API key (get from your provider)
EMAIL_API_KEY=your_api_key_here

# Email FROM address (must be verified with your provider)
EMAIL_FROM=chatbot@webvello.com

# Your email address where leads will be sent
EMAIL_TO=your-email@webvello.com

# Optional: Webhook URL (if using webhook provider)
# WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/xxxxx/

# ============================================
# ADMIN AUTHENTICATION
# ============================================

# Admin password for admin dashboard (CHANGE THIS IN PRODUCTION!)
# Default is 'admin123' if not set
ADMIN_PASSWORD=your-secure-password-here

# Optional: Session secret for auth (auto-generated if not set)
# SESSION_SECRET=your-random-secret-here

# ============================================
# CALENDLY INTEGRATION
# ============================================

# Your Calendly username (the part after calendly.com/)
# Example: If your Calendly URL is https://calendly.com/john-doe/discovery-call
# Then set: NEXT_PUBLIC_CALENDLY_USERNAME=john-doe
NEXT_PUBLIC_CALENDLY_USERNAME=your-username-here

# Optional: Calendly event type (defaults to 'discovery-call')
# Example: NEXT_PUBLIC_CALENDLY_EVENT_TYPE=discovery-call
NEXT_PUBLIC_CALENDLY_EVENT_TYPE=discovery-call
